# Q3: Doubly Linked List Implementation

This program implements a menu-driven doubly linked list in C, supporting the same operations as Q1 (1-15), but using a doubly linked list.

## Usage
- Compile with: `gcc main.c -o dll`
- Run: `./dll`
- Follow the menu prompts.
